---
import styles from "./one-to-one-ratio.module.css";

interface OneToOneRatioProps {
	class?: string;
}

const { class: className } = Astro.props as OneToOneRatioProps;
---

<div class={`${styles.parent} ${className}`}>
	<div class={styles.child}><slot /></div>
</div>
